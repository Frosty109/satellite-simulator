# Issues — 2026-05-27

### False Quirks Mode warning in browser console

**Symptom:** Browser console showed `"This page is in Quirks Mode. Page layout may be impacted."` when the dev server was running.

**Investigation:** Checked `index.html` — `<!DOCTYPE html>` was already present on line 1.

**Resolution:** False alarm. The warning was likely emitted by a browser extension or the DevTools panel itself, not the application page. No action required.

---

### No other issues this session

WASM bridge sanity check passed on the first run. All four validation checks (state count,
position magnitude, velocity magnitude, epoch span) were within expected ranges.
